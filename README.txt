Receipt Bonsai (cute PWA) — no Mac required

Upload these files to GitHub Pages (repo root).
Then open your Pages URL in Safari and Add to Home Screen.

Data is stored locally in your browser. Use Settings → Export JSON backup sometimes.
